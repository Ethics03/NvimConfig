"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const parse_error_1 = require("./errors/parse.error");
const parse_function_1 = require("./parse.function");
/**
 * Tries to parse the recieved value into a `Date` object with the registered parsers,
 * throws an instance of `ParsoParseError` error if the parsing attempt fails.
 *
 * @param value any string or number to parse into a Date
 * @param parseOptions
 */
function parseOrThrow(value, parseOptions = {}) {
    const parseResult = parse_function_1.parse(value, parseOptions);
    if (parseResult == null) {
        throw new parse_error_1.ParsoParseError(value);
    }
    return parseResult;
}
exports.parseOrThrow = parseOrThrow;
//# sourceMappingURL=parse-or-throw.function.js.map
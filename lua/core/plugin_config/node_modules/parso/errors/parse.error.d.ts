/**
 * Thrown when Parso could not parse the recieved value into Date.
 */
export declare class ParsoParseError extends Error {
    recievedValue: string | number | Date;
    readonly name: string;
    constructor(recievedValue: string | number | Date);
}
